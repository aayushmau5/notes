What is SqLite?

It seems like an embed-able database that runs in-memory.

It's a database engine(what does this mean?)

It has a file format as well.

It's in-process library.
It's an embedded SQL database engine.

Sqlite reads and writes directly to ordinary disk files.

There is sqlite(or sqlite3) program and a SQLite library. You run the sqlite3 program. Commands entered in sqlite3 are passed to the SQLite library.

What is litefs?
What is WAL(write ahead logs) mode?
